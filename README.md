# DPEX Smart Contracts
Contracts of DPEX.

Their docs at https://docs.dpex.io/

## Install Dependencies
If npx is not installed yet:
`npm install -g npx`

Install packages:
`npm i`

## Compile Contracts
`npx hardhat compile`

## Run Tests
`npx hardhat test`
